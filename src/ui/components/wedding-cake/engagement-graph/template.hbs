<div class="dashboard__sub-numbers">
    <div class="dashboard__sub-numbers--graph">
      <svg class="chart">
          <g>
              <rect
                  class="background"
                  width="100%"
                  height="100%"
                  y="40"
              ></rect>

              <rect
                  class="bar"
                  height="100%"
                  width="{{weightedEngagementNumber}}%"
                  y="40"
              ></rect>

              <text
                x="{{weightedEngagementNumber}}%"
                y="77"
                font-family="sans-serif"
                font-size="48px"
              >
                {{weightedEngagementNumber}}
                </text>
          </g>

            <g>
                {{#each weightedMilestones key="@index" as |milestone|}}
                    <line
                        style="stroke-width: 1"
                        x1="{{milestone.weighted}}%"
                        x2="{{milestone.weighted}}%"
                        y1="0"
                        y2="100%"
                    ></line>

                    <text
                        x="{{milestone.weighted}}%"
                        y="40"
                        font-family="sans-serif"
                        font-size="48px"
                    >
                        {{milestone.value}}
                    </text>
                {{/each}}
            </g>
      </svg>
    </div>
  </div>
